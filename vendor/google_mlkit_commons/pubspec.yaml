name: google_mlkit_commons
description: "A Flutter plugin with commons files to implement google's standalone ml kit made for mobile platform."
version: 0.12.0
homepage: https://github.com/flutter-ml/google_ml_kit_flutter
repository: https://github.com/flutter-ml/google_ml_kit_flutter/tree/master/packages/google_mlkit_commons

# Vendored copy: SDK constraints relaxed from upstream (>=3.8.0 / Flutter
# >=3.32.0) — the Dart source uses no post-3.0 language features and this
# project builds on Dart 3.1 / Flutter 3.13.
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.google_mlkit_commons
        pluginClass: GoogleMlKitCommonsPlugin
      ios:
        pluginClass: GoogleMlKitCommonsPlugin
