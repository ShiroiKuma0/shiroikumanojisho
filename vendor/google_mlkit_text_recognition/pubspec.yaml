name: google_mlkit_text_recognition
description: "A Flutter plugin to use Google's ML Kit Text Recognition to recognize text in any Chinese, Devanagari, Japanese, Korean and Latin character set."
version: 0.16.0
homepage: https://github.com/flutter-ml/google_ml_kit_flutter
repository: https://github.com/flutter-ml/google_ml_kit_flutter/tree/master/packages/google_mlkit_text_recognition

# Vendored copy: SDK constraints relaxed from upstream (>=3.8.0 / Flutter
# >=3.32.0) — the Dart source uses no post-3.0 language features and this
# project builds on Dart 3.1 / Flutter 3.13. Commons is the sibling vendored
# package, not the hosted release.
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_commons:
    path: ../google_mlkit_commons

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.google_mlkit_text_recognition
        pluginClass: GoogleMlKitTextRecognitionPlugin
      ios:
        pluginClass: GoogleMlKitTextRecognitionPlugin
