<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="shiroikuma.jisho">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Inert at the targetSdk 32 held here: FGS types are enforced
         from targetSdk 34. Declared now because AutomationDataService
         is foregroundServiceType="specialUse", and the day targetSdk
         rises WITHOUT this the service is refused at startForeground()
         — an automation export that dies silently rather than loudly.
         (compileSdk is 36, so aapt2 accepts the specialUse literal.) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="白い熊 辞書"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:enableOnBackInvokedCallback="false"
        xmlns:tools="http://schemas.android.com/tools"
        tools:replace="android:label">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                    the Android process has started. This theme is visible to the user
                    while the Flutter UI initializes. After that, this theme continues
                    to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Used for opening the creator via share menu -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
             <!-- Used for web search -->
            <intent-filter>
                <action android:name="android.intent.action.WEB_SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <!-- Study-video import from shiroikuma-jiyudoga ("Study in
                 jisho"). jiyudoga fires this explicitly by component, so
                 the filter is not required for it — it exists so the
                 intent can also be fired from adb for testing. -->
            <intent-filter>
                <action android:name="shiroikuma.jisho.intent.action.STUDY_VIDEO" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <!--Used for image sharing -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
             <!-- Used for Plex support -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="http"/>
                <data android:scheme="https"/>
                <data android:scheme="smb"/>
                <data android:scheme="rtmp"/>
                <data android:scheme="rtmpe"/>
                <data android:scheme="rtmps"/>
                <data android:scheme="rtp"/>
                <data android:scheme="rtsp"/>
                <data android:scheme="mms"/>
                <data android:scheme="mmsh"/>
                <data android:scheme="icyx"/>
                <data android:scheme="httplive"/>
                <data android:scheme="udp"/>
                <data android:scheme="vlc"/>
                <data android:mimeType="video/*"/>
                <data android:mimeType="audio/*"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <data android:mimeType="text/plain"/>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- 保存復元 automation contract v2: headless state export,
             driven by 自由作業盤. No android:permission by design — in
             v2 this receiver is the deliberately UNAUTHENTICATED half
             of the surface: it only ever writes where it was told to
             and reports what it did. Everything that moves data
             through a caller-supplied descriptor lives behind
             AutomationProvider below, which knows who is calling.

             CANCEL_EXPORT must be reachable from outside, so it is
             routed through this exported receiver and signals the
             service internally — the service is (correctly)
             exported="false" and a third-party app cannot start it. -->
        <receiver
            android:name="shiroikuma.jisho.StateExportReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="shiroikuma.jisho.action.EXPORT_STATE" />
                <action android:name="shiroikuma.jisho.action.LIST_CATEGORIES" />
                <action android:name="shiroikuma.jisho.action.CANCEL_EXPORT" />
            </intent-filter>
        </receiver>
        <service
            android:name="shiroikuma.jisho.StateExportService"
            android:exported="false" />

        <!-- v2 §2a — the data door. A provider rather than another
             broadcast action because a broadcast cannot tell you who
             sent it, and a caller supplies the descriptor an export is
             written into. No android:permission: a custom one would
             have to be "normal" (any app may hold it) or "signature"
             (impossible — every app in this family has its own
             keystore), and neither buys anything the pinned caller
             check in AutomationCallers does not. -->
        <provider
            android:name="shiroikuma.jisho.AutomationProvider"
            android:authorities="${applicationId}.automation"
            android:exported="true" />
        <service
            android:name="shiroikuma.jisho.AutomationDataService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Automation data export/import" />
        </service>

        <!-- Capability discovery, read WITHOUT waking this app: a
             frozen app cannot be asked anything, and 白い熊 freezes
             aggressively. 応用管理 reads these across every installed
             package with MATCH_UNINSTALLED_PACKAGES |
             MATCH_DISABLED_COMPONENTS. An app without them is simply
             not offered for data backup or restore. -->
        <meta-data android:name="shiroikuma.automation.contract"   android:value="2" />
        <meta-data android:name="shiroikuma.automation.format"     android:value="1" />
        <meta-data android:name="shiroikuma.automation.min_format" android:value="1" />
        <!-- Keeps the process alive/unfrozen during long imports
             (scanned-PDF OCR) when the app is backgrounded. -->
        <service
            android:name="shiroikuma.jisho.ImportForegroundService"
            android:exported="false" />
        <!-- Used for dictionary search via global context menu option -->
        <service android:name="com.gomes.nowplaying.NowPlayingListenerService"
            android:label="NowPlayingListenerService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

         <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true" tools:ignore="Instantiatable">
        <intent-filter>
            <action android:name="android.media.browse.MediaBrowserService" />
        </intent-filter>
        </service>
        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true" tools:ignore="Instantiatable">
        <intent-filter>
            <action android:name="android.intent.action.MEDIA_BUTTON" />
        </intent-filter>
        </receiver> 
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
        <provider
            android:name="com.pichillilorenzo.flutter_inappwebview_android.InAppWebViewFileProvider"
            android:authorities="${applicationId}.flutter_inappwebview.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
        <!-- Don't delete the meta-data below.
                This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Used for AnkiDroid API -->
    <queries>
        <package android:name="com.ichi2.anki" />
        <!-- 保存復元 v2 §4: without these, the reply broadcast's
             setPackage() fails SILENTLY on Android 11+ — the export
             runs, writes correctly, and is never heard of. (This app
             also holds QUERY_ALL_PACKAGES, which would cover it, but
             the contract names them explicitly and the permission is
             not something to depend on here.) -->
        <package android:name="shiroikuma.oyokanri" />
        <package android:name="shiroikuma.jiyusagyoban" />
    </queries>
</manifest>
