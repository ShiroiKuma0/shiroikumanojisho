org.gradle.jvmargs=-Xmx4608m
android.useAndroidX=true
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Temporary escape hatch (pairs with newDsl=false): legacy plugins in
# this dependency graph still apply kotlin-android themselves, which
# AGP 9's built-in Kotlin rejects. Remove both flags when Flutter and
# the plugin set support the new DSL / built-in Kotlin.
android.builtInKotlin=false
